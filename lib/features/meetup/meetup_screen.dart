import 'package:flutter/material.dart';
import '../../app/theme.dart';
import '../../core/widgets/merc_widgets.dart';

class MeetupScreen extends StatefulWidget { const MeetupScreen({super.key}); @override State<MeetupScreen> createState()=>_MeetupScreenState(); }
class _MeetupScreenState extends State<MeetupScreen> { bool sharing=false; @override Widget build(BuildContext context)=>Scaffold(appBar:AppBar(title:const Text('Ritiro a mano',style:TextStyle(fontWeight:FontWeight.w900))),body:ListView(padding:const EdgeInsets.all(18),children:[Container(height:190,decoration:BoxDecoration(borderRadius:BorderRadius.circular(24),gradient:const LinearGradient(colors:[Color(0xFF143247),Color(0xFF091521)])),child:const Center(child:Icon(Icons.location_on_rounded,size:64,color:MercDealTheme.blue))),const SizedBox(height:15),const Text('Ecco dove devi incontrare il venditore',style:TextStyle(fontSize:21,fontWeight:FontWeight.w900)),const SizedBox(height:7),const Text('Il punto d’incontro viene scelto dal venditore. L’indirizzo privato non viene mostrato automaticamente.',style:TextStyle(color:Colors.white54,height:1.35)),const SizedBox(height:15),Container(padding:const EdgeInsets.all(15),decoration:BoxDecoration(color:MercDealTheme.card,borderRadius:BorderRadius.circular(19)),child:const Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text('📍 Punto concordato',style:TextStyle(fontWeight:FontWeight.w900)),SizedBox(height:6),Text('Punto d’incontro pubblico · mappa disponibile dopo l’acquisto',style:TextStyle(color:Colors.white60))])),const SizedBox(height:12),SwitchListTile(contentPadding:EdgeInsets.zero,value:sharing,onChanged:(v)=>setState(()=>sharing=v),activeThumbColor:MercDealTheme.green,title:const Text('Condivisione posizione temporanea',style:TextStyle(fontWeight:FontWeight.w800)),subtitle:Text(sharing?'🟢 Posizione condivisa per questo incontro':'La posizione resta privata finché non dai il consenso')),if(sharing)Container(padding:const EdgeInsets.all(14),decoration:BoxDecoration(color:const Color(0xFF102B25),borderRadius:BorderRadius.circular(18)),child:const Text('La condivisione termina quando l’affare viene concluso o allo scadere della finestra d’incontro.',style:TextStyle(color:MercDealTheme.green,fontWeight:FontWeight.w700))),const SizedBox(height:15),GlowButton(label:'Segna: sono arrivato',icon:Icons.check_circle_outline,onPressed:()=>ScaffoldMessenger.of(context).showSnackBar(const SnackBar(content:Text('Arrivo registrato per questo incontro.'))))])); }
