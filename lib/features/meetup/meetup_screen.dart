import 'package:flutter/material.dart';
import '../../app/theme.dart';
import '../../core/widgets/merc_widgets.dart';
class MeetupScreen extends StatefulWidget{const MeetupScreen({super.key});@override State<MeetupScreen> createState()=>_MeetupScreenState();}
class _MeetupScreenState extends State<MeetupScreen>{bool sharing=false,arrived=false;@override Widget build(BuildContext c)=>Scaffold(appBar:AppBar(title:const Text('Ritiro a mano',style:TextStyle(fontWeight:FontWeight.w900))),body:ListView(padding:const EdgeInsets.all(18),children:[Container(height:200,decoration:BoxDecoration(borderRadius:BorderRadius.circular(25),gradient:const LinearGradient(colors:[Color(0xFF14384C),Color(0xFF0A1522)])),child:const Center(child:Icon(Icons.location_on_rounded,size:72,color:MercDealTheme.blue))),const SizedBox(height:15),const Text('Ecco dove devi incontrare il venditore',style:TextStyle(fontSize:21,fontWeight:FontWeight.w900)),const SizedBox(height:6),const Text('Il punto d’incontro viene deciso dal venditore dopo l’acquisto. L’indirizzo privato non viene mostrato automaticamente.',style:TextStyle(color:Colors.white54,height:1.35)),const SizedBox(height:14),Container(padding:const EdgeInsets.all(15),decoration:BoxDecoration(color:MercDealTheme.card,borderRadius:BorderRadius.circular(19)),child:const Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text('📍 Punto concordato',style:TextStyle(fontWeight:FontWeight.w900)),SizedBox(height:5),Text('Punto pubblico · mappa disponibile nell’ordine',style:TextStyle(color:Colors.white38))])),const SizedBox(height:10),SwitchListTile(contentPadding:EdgeInsets.zero,value:sharing,onChanged:(v)=>setState(()=>sharing=v),activeThumbColor:MercDealTheme.green,title:const Text('Condividi posizione temporaneamente',style:TextStyle(fontWeight:FontWeight.w800)),subtitle:Text(sharing?'🟢 Posizione condivisa':'La posizione resta privata finché non dai consenso')),if(sharing)Container(padding:const EdgeInsets.all(13),decoration:BoxDecoration(color:MercDealTheme.green.withValues(alpha:.08),borderRadius:BorderRadius.circular(16)),child:const Text('La condivisione termina quando l’affare viene concluso o dopo la finestra di 10 minuti.',style:TextStyle(color:MercDealTheme.green,fontSize:12,fontWeight:FontWeight.w700))),const SizedBox(height:13),GlowButton(label:arrived?'✓ Sono arrivato':'Segna: sono arrivato',icon:Icons.check_circle_outline,onPressed:()=>setState(()=>arrived=true)),if(arrived)...[const SizedBox(height:12),Container(padding:const EdgeInsets.all(15),decoration:BoxDecoration(color:const Color(0xFF102B25),borderRadius:BorderRadius.circular(18)),child:const Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text('🎉 Arrivo registrato',style:TextStyle(color:MercDealTheme.green,fontWeight:FontWeight.w900)),SizedBox(height:5),Text('Quando entrambe le persone sono arrivate parte la finestra finale di 10 minuti.',style:TextStyle(color:Colors.white60))]))]]));}
